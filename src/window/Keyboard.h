#ifndef RIGEL_KEYBOARD_H
#define RIGEL_KEYBOARD_H

#include "Window.h"

namespace rigel {
    class Keyboard {
    public:
        static bool isKeyPressed(unsigned int key);
    };
}

#endif //RIGEL_KEYBOARD_H
